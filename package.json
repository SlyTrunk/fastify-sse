{
  "name": "sly-fastify-sse",
  "version": "0.1.6",
  "description": "Slytrunk Fork - Provide Server-Sent Events to Fastify",
  "main": "index.js",
  "scripts": {
    "test": "tap test/*.js --coverage --100",
    "test:coverage": "tap test/*.js --coverage --100 --coverage-report=html"
  },
  "author": "Lolo_32",
  "license": "MIT",
  "keywords": [
    "Fastify",
    "server-sent events",
    "sse"
  ],
  "devDependencies": {
    "fastify": "^2.3.0",
    "request": "^2.81.0",
    "tap": "^10.7.2"
  },
  "dependencies": {
    "fast-safe-stringify": "^1.2.0",
    "fastify-plugin": "^1.6.0"
  }
}
